# Отчёты Office Supply

Проект **Office Supply** — система отчётов по заказам и договорам организаций на Yii2.  
Цель проекта — автоматизация анализа данных и отображение статистики в удобном виде.

---

## 📑 Отчёты

В проекте реализованы следующие отчёты:

1. **Цена ручек "черная, гелевая"** — показывает продукцию с описанием "черная" и "гелевая".
2. **Продукция по договорам в сентябре** — список продукции по договорам, заключённым в сентябре.
3. **Заказы ИП Малиновский А.С.** — список заказов конкретной организации.
4. **Количество заказов организаций** — количество заказов по каждой организации.
5. **Организации с более чем 2 договорами** — список организаций, имеющих более двух договоров.

---

## 🛠 Установка и запуск

### 1. Клонировать проект

```bash
git clone https://github.com/Biomorinnio/office-supply.git
cd office-supply
```

### 2. Установить зависимости

```bash
composer install
```

### 3. Создать базу данных

1. Открой phpMyAdmin или MySQL и создай базу:

```bash
CREATE DATABASE office_supply CHARACTER SET utf8 COLLATE utf8_general_ci;
```

### 4. Импортировать дамп базы

В phpMyAdmin → Импорт → выбрать файл office_supply.sql.

### 5. Настроить подключение к базе

Открой файл:

```bash
config/db.php
```

и укажи свои параметры:

```bash
return [
    'class' => 'yii\db\Connection',
    'dsn' => 'mysql:host=localhost;dbname=office_supply',
    'username' => 'root',
    'password' => '',
    'charset' => 'utf8',
];
```

### 6. Запустить проект

```bash
php yii serve
```

Открой браузер и перейди по адресу:
http://localhost:8080

### 📂 Структура проекта

```bash
office-supply/
│
├── assets/
├── config/
├── controllers/
├── models/
├── reports/
├── views/
├── vendor/
├── office_supply.sql
└── README.md
```

### 🧾 Использование

В проекте доступны отчёты через меню навигации.
Каждый отчёт отображает таблицу с данными и фильтрами.

### 📌 Автор

Biomorinnio (Крысанов Д.О.)
